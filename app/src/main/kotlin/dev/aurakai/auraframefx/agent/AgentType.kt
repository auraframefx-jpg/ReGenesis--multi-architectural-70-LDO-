package dev.aurakai.auraframefx.agent

/**
 * Agent types for the orchestration layer.
 * Maps to the domain-level AgentType in genesis.models.
 */
enum class AgentType {
    AURA,
    KAI,
    GENESIS,
    CASCADE
}
